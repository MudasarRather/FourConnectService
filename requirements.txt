fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.29
alembic>=1.13.1
psycopg2-binary>=2.9.9
python-jose[cryptography]>=3.3.0
passlib[bcrypt,argon2]>=1.7.4
argon2-cffi>=23.1.0
python-multipart>=0.0.9
python-dotenv>=1.0.1
pydantic[email]>=2.7.0
pydantic-settings>=2.2.1
huggingface_hub>=0.20.0

# ── Reporting stack ────────────────────────────────────────────────────────
# WeasyPrint renders the unique branded PDFs for HR Attendance Reports.
# On Windows it needs GTK3 DLLs in PATH — vendor/gtk-runtime/ ships them and
# app/utils/gtk_bootstrap.py prepends that dir to PATH at import time.
# On Linux/macOS the system Pango/Cairo are used and the bootstrap is a no-op.
weasyprint>=68.0
# openpyxl: cell-level styling, theming, conditional formats, formulas.
# xlsxwriter: native chart objects, sparklines, complex number formats.
# Both are used side-by-side — see app/utils/hr/attendance_reports/excel/.
openpyxl>=3.1.5
xlsxwriter>=3.2.0
pypdf>=4.0.0
